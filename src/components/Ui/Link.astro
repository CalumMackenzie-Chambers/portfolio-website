---
interface Props {
  href: string;
  size?: "sm" | "md" | "lg";
  block?: boolean;
  style?: "transparentPrimary" | "transparentInverted" | "btnPrimary" | "btnInverted" | "nav";
  class?: string;
  [x: string]: any;
}

const { href, block, size = "lg", style = "btnPrimary", class: className, ...rest } = Astro.props;

const sizes = {
  lg: "px-5 py-2.5",
  md: "px-4 py-2",
  sm: "",
};

const styles = {
  transparentPrimary:
    "rounded-lg border-2 border-purple-700 bg-transparent text-purple-700 hover:bg-purple-100 dark:border-purple-300 dark:text-purple-300 dark:hover:bg-gray-700",
  transparentInverted:
    "rounded-lg border-2 border-gray-400 bg-transparent text-gray-600 hover:bg-purple-100 dark:border-gray-400 dark:text-gray-400 dark:hover:bg-gray-700",
  btnPrimary: "rounded-full bg-black text-white hover:bg-gray-800  border-2 border-transparent",
  btnInverted: "rounded-full bg-white text-black   border-2 border-transparent",
  nav: "font-medium text-xl flex py-4 text-gray-800 hover:text-purple-700 dark:text-gray-200 dark:hover:text-purple-300 lg:text-base lg:px-3 lg:py-2 lg:font-normal",
};
---

<a
  href={href}
  {...rest}
  class:list={[
    "text-center transition focus-visible:ring-2 ring-offset-2 ring-gray-200",
    block && "w-full",
    sizes[size],
    styles[style],
    className,
  ]}
  ><slot />
</a>
